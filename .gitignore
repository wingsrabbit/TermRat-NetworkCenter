# 临时来源（两个源仓库 clone，取材用、合并完删，不入库）
origin/

# 本地凭据 / 部署记录（含真实密钥，永不入库）
*.local.md

# cd 设计原型压缩包（已解压到 docs/design-prototype/）
ONC.zip

# 依赖与构建产物
node_modules/
dist/
build/
__pycache__/
*.pyc
.venv/
venv/

# 运行数据（SQLite 等）
data/
*.sqlite
*.sqlite3
*.db

# 环境与系统
.env
.env.*
!.env.example
.DS_Store
